
import { Automaton } from "../../utils/automaton"
import { AutomatonAlgorithm } from "./Algorithm"
import { Main } from "./styles"

interface AutomatonResultProps {
  expression: string[]
}

export const AutomatonResult = ({ expression }: AutomatonResultProps) => {
  // var teste: any[] =[]
  // teste = AutomatonAlgorithm(expression)

  // var result = Automaton.Result()
  // result.map((el)=>{
  //   console.log(el)
  // })
  return (
    <Main>
    </Main>
  )
}