import styled from 'styled-components'

export const MainContainer = styled.div`
  display: flex;
  flex-direction: column;

  color: var(--text);
  font-weight: 500;
  font-size: 1rem;
 
  position: absolute;
  bottom: .5rem;
  left: 2rem;

  line-height: 1.5rem
`