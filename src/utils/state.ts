import { ReactNode } from "react";
export class Rules{
  private q: number | undefined;
  private a: string | undefined;
  private p: number | undefined;
  private count: number | undefined;
  
  public settrasitionFunciton(q1:number, a1:string, p1:number){
    if(this.count == 0){}
  };
  public *gettrasitionFunciton(q1:number, a1:string, p1:number){};
  public getCount(){}
}
